CREATE TABLE customer (id BIGINT AUTO_INCREMENT, login VARCHAR(50) NOT NULL UNIQUE, mail VARCHAR(255) NOT NULL, name VARCHAR(255) NOT NULL, password VARCHAR(50) NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
